<h2>My PRIVATE Workouts</h2>

<div class="page-top"> 
</div>
<div class="container">
  <div class="row">
    <div class="col">
    </div>
<div class="col-6">

{{#each workouts}}

 <section class="card-template">

    {{#eq user.id ../userId}}

        <a href="/workout-detail/{{id}}">{{title}}</a>
        <br>

        <div>
                <img src={{user.photo}} alt="user profile pic" style="height: 40px; width: 40px; border-radius:50px;"> 
                <p> by <a href="/profile/{{user.id}}">{{user.username}}</a>
                </p>
        <div>

        <br>

        Description: {{description}}
        <br>
        <br>
        Main Goal:  {{workoutGoals}}
        <br>

        {{!-- {{#each exercises}}
        <p>{{_id}}</p>
        {{/each}} --}}

        <a href="/workout/{{id}}">Add an exercise</a>

        <form action="/workout-delete/{{id}}" method="POST">
            <button type="submit">Delete</button>
        </form>

        <hr />
    {{/eq}}

<hr>
</div>
    </section>
  {{/each}} 
  </div>
    <div class="col">
    </div>
  </div>
</div>
<br>

</hr>
<div class="container">
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4 d-flex justify-content-center">
    </div>
    <div class="col-md-4"></div>
  </div>
</div>








